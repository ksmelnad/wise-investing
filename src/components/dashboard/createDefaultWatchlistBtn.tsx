"use client";

import React from "react";
import { Button } from "@/components/ui/button";

const CreateDefaultWatchlistBtn = () => {
  return <Button>Activate Watchlists</Button>;
};

export default CreateDefaultWatchlistBtn;
